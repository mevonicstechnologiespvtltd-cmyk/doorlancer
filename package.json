{
  "name": "doorlance-workspace",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "description": "Doorlance \u2014 unified backend + web app served from a single Node process (Firebase Firestore/Storage backed)",
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix mobile-app install",
    "build": "npm --prefix mobile-app run build:web",
    "start": "npm run build && npm --prefix backend start",
    "start:fast": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev",
    "install:all": "npm install"
  }
}

